import merge from 'lodash/merge';




const nutritionReducer = (state = {}, action) => {
    Object.freeze(state);
    let newSt;

    switch (action.type) {
        default:
            return state;
    }
};

export default nutritionReducer;
